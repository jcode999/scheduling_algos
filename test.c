#include <stdlib.h>
#include <stdio.h>

void insert(int** head )
{
     int * newNode = malloc(sizeof(int));
     *newNode = 
     *head = newNode;
}

int main(){
    int* new_node = malloc(sizeof(int));
    *new_node = 10;
    int ** list_head;
    printf("%d\n",*new_node);

    free(new_node);
    return 0;

}



